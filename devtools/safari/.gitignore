# Build artifacts — regenerable from source via sync.sh / convert.sh.
# `extension/` is assembled by sync.sh from ../ (the Chrome extension); the
# Xcode project is generated by convert.sh. Neither is source of truth.
extension/
SwiflowDevTools/
*.zip
